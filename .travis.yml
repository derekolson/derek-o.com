language: node_js
node_js:
  - "6.4"

branches:
  only:
    - master

install:
  - bundle install
  - npm install -g firebase-tools

script:
  - jekyll build

after_success:
  - firebase deploy --token=${FIREBASE_TOKEN}
