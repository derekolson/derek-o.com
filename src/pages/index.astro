---
import Layout from '../layouts/Layout.astro';
import { site } from '../config';

import { Image } from "astro:assets";
import { getCollectionSorted } from "../lib/utils";

const projects = await getCollectionSorted("projects");

---

<Layout title={site.tagline} description={site.description}>
	<div class="description">
		<h2>{ site.description }</h2>
		<a href="/about">About →</a>
	</div>
	<div class="projects">
		{ projects.map((project, idx) => (
			<div class={`project ${project}`}>
				<a href={ `/${project.collection}/${project.id}`}>
					<figure style={{ backgroundColor: project.data.color }}>
						<Image src={project.data.cover} widths={[540, 720, project.data.cover.width]} alt={project.data.coverAlt} loading={idx < 2 ? 'eager' : 'lazy'} />
					</figure>
					<h2>{ project.data.title }</h2>
				</a>
			</div>
		))}
	</div>
</Layout>
